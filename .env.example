# Example environment file for NASA Data Explorer Backend API
# Copy this file to .env and adjust values as needed.

# Server
PORT=8000
NODE_ENV=development

# NASA API
# Obtain a key from https://api.nasa.gov
NASA_API_KEY=

# CORS origins (production only)
# Comma-separated list of allowed origins (no spaces), e.g.:
# FRONTEND_ORIGIN=https://app.example.com,https://admin.example.com
FRONTEND_ORIGIN=
